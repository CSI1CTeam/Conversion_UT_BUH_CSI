Если ЗначениеЗаполнено(Источник.СерияНоменклатуры) И ЗначениеЗаполнено(Источник.СерияНоменклатуры.НомерГТД) Тогда
	Значение = Источник.СерияНоменклатуры.СтранаПроисхождения;
Иначе
	Значение = Неопределено;
	Отказ    = Истина;
КонецЕсли;