//Если НЕ ЗначениеЗаполнено(Источник.ДатаОплаты) Тогда
//	Значение = Источник.Дата;
//Иначе
//	Значение = Источник.ДатаОплаты;
//КонецЕсли

Если НачалоДня(Источник.Дата) = НачалоДня(Источник.ДатаОплаты) Тогда
	Значение = Источник.Дата;
Иначе
	Значение = КонецДня(Источник.ДатаОплаты);
КонецЕсли;